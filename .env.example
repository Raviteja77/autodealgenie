# AutoDealGenie Environment Configuration for Docker Compose
# Copy this file to .env and update with your actual values
# This file is used by docker-compose.yml to populate environment variables

# OpenAI API Keys (for AI features)
# Get your key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your-openai-api-key-here
OPENROUTER_API_KEY=your-openrouter-api-key-here

# Security - Generate a strong secret key for production
# Generate with: openssl rand -hex 32
SECRET_KEY=dev-secret-key-change-in-production-min-32-chars-long

# Database Password (change in production!)
POSTGRES_PASSWORD=autodealgenie_password

# NOTE: For local development without Docker Compose:
# - Backend service config: See backend/.env.example
# - Frontend service config: See frontend/.env.example
# - GCP deployment config: See backend/.env.dev.example and backend/.env.prod.example
